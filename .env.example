# Real values live only on the box at /srv/codeknight-infra/.env (gitignored there).
# Image tags are updated by the reusable deploy workflow.
CODEKNIGHT_TAG=latest
NES_TAG=latest
# codeknight feature flag: set to exactly "true" to publish the writing section.
WRITING_ENABLED=
